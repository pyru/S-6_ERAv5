# Shards are content-hashed byte-for-byte: never let git rewrite them.
*.bin binary

# Everything else is LF in the working tree on every platform, so the
# artifacts a grader clones are the artifacts the run produced.
* text=auto eol=lf
*.jsonl text eol=lf
*.json text eol=lf
*.log text eol=lf
